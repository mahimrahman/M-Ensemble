/**
 * The app's view of the PHASE 0 contract.
 *
 * Types are owned by `packages/shared` so the server and the app can never
 * disagree. Import from `@/types` inside the app; edit in the shared package.
 */
export * from '@m-ensemble/shared';
