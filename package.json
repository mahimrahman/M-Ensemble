{
  "name": "m-ensemble",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "author": "Mahimur Rahman Khan",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:api-only": "node scripts/dev.mjs --no-mobile --no-admin",
    "dev:server": "npm run dev --workspace @m-ensemble/server",
    "dev:mobile": "npm run start --workspace @m-ensemble/mobile",
    "dev:admin": "npm run dev --workspace @m-ensemble/admin",
    "build": "npm run build --workspace @m-ensemble/shared && npm run build --workspace @m-ensemble/server && npm run build --workspace @m-ensemble/admin",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "seed": "npm run seed --workspace @m-ensemble/server",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md}\""
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.6.3"
  }
}
